{
  "name": "trailconditions.api",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\"",
    "installPackages": "npm install"
  },
  "dependencies": {
    "axios": "^0.19.2",
    "osmtogeojson": "^3.0.0-beta.4",
    "querystring": "^0.2.0"
  },
  "devDependencies": {
    "@azure/functions": "^1.0.2-beta2",
    "typescript": "^3.3.3"
  }
}
